import { Placeholder } from '@/ui/components/Placeholder';

// Placeholder for the Week tab (DESIGN §7.1). Slice 9 builds it (docs/BUILD_PLAN.md).
export default function WeekScreen() {
  return <Placeholder title="Week" />;
}
