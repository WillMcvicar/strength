import { Placeholder } from '@/ui/components/Placeholder';

// Placeholder for the More tab (DESIGN §7.1). Slice 13–14 builds it (docs/BUILD_PLAN.md).
export default function MoreScreen() {
  return <Placeholder title="More" />;
}
