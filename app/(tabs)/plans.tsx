import { Placeholder } from '@/ui/components/Placeholder';

// Placeholder for the Plans tab (DESIGN §7.1). Slice 5 builds it (docs/BUILD_PLAN.md).
export default function PlansScreen() {
  return <Placeholder title="Plans" />;
}
