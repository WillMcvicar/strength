import { Stack } from 'expo-router';

// DESIGN §2.2: this layout will host the providers, DB init, migrations and the
// disclaimer gate (FR-5). Build-plan step 7 replaces the placeholder stack.
export default function RootLayout() {
  return <Stack />;
}
