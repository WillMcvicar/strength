// Public surface of the pure domain layer (DESIGN §2.2). Re-export the core modules from here
// as they land: units, rounding, dates, loads, e1rm, schedule/*, reviews, doubleProgression,
// prs, volume, deload, taper, types.
export {};
