// Types for drizzle-kit's generated `migrations.js` bundle (driver: 'expo'). Hand-written;
// drizzle-kit never touches this file.
import type { MigrationBundle } from '../migrate';

declare const bundle: MigrationBundle;
export default bundle;
